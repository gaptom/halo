use DateTime;

my $dt = DateTime->new(
    year   => 2024,
    month  => 2, # 月份直接从1开始计数，2代表二月
    day    => 1,
    hour   => 0,
    minute => 0,
    second => 0,
);

# DateTime对象可以直接用于获取UNIX时间戳
my $specified_timestamp = $dt->epoch;
